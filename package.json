{
  "name": "mantracode",
  "version": "1.0.1",
  "author": "Nishant Chauhan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AspireCalc/MantraCode.git"
  },
  "main": "index.js",
  "description": "Mantram an agentic code model, AI",
  "license": "ISC",
  "scripts": {
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
    "dev:server": "bun run --hot packages/server/src/index.ts",
    "build:cli": "bun run --filter @aspirenx/mantracode build",
    "link:cli": "bun run build:cli && cd packages/cli && bun link",
    "web": "cd web && bun run dev"
  },
  "type": "commonjs",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@opentui/core-linux-arm64": "0.2.10",
    "dotenv": "^17.4.2"
  }
}
